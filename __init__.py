"""The XTTS TTS integration."""
DOMAIN = "xtts_tts"

async def async_setup(hass, config):
    """Set up the XTTS TTS component."""
    return True

async def async_setup_entry(hass, config_entry):
    """Set up XTTS TTS from a config entry."""
    return True